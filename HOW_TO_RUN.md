
هذا الدليل يشرح خطوات تشغيل المشروع على جهازك.  


## 🪟 ويندوز (Windows)

### 1. فك ضغط الملف
- بعد تنزيل الملف من GitHub (مثل `remote-dev-week1-main.zip`)، انقر عليه بزر الماوس الأيمن.
- اختر **"استخراج هنا"** أو **"Extract All..."**.
- سيُنشئ مجلدًا جديدًا (مثل `remote-dev-week1-main`).

### 2. افتح Terminal في مجلد المشروع
- اذهب إلى المجلد الذي فككته.
- انقر بزر الماوس الأيمن داخل المجلد → اختر **"فتح في Terminal"**  
  (أو **"Open in Windows Terminal"** / **"Open PowerShell window here"**).
- افتح باستخدام VS code

### 3. نفّذ الأوامر التالية **بالترتيب**:

```cmd
:: إنشاء بيئة افتراضية
python -m venv venv

:: تفعيل البيئة الافتراضية
venv\Scripts\activate

:: تثبيت الحزم المطلوبة
pip install -r requirements.txt

:: إعداد قاعدة البيانات
python manage.py migrate

:: (اختياري) إنشاء حساب مدير
python manage.py createsuperuser

:: تشغيل الخادم
python manage.py runserver